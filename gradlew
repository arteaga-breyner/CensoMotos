#!/bin/sh
# Gradle wrapper - este archivo no se usa en la compilación en GitHub Actions
# La compilación usa Gradle instalado directamente por gradle/actions/setup-gradle
echo "Use: gradle assembleDebug"
